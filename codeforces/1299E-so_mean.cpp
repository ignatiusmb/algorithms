#include <bits/stdc++.h>
using namespace std;

int main()
{
   // optimizations and rulesets
   ios_base::sync_with_stdio(false);
   cin.tie(0);

   // data structures and variables
   int t, n, i, j, res;

   cin >> t;
   for (i = 0; i < t; i++)
   {
      cin >> n;
      int a[n];
      for (j = 0; a[j]; j++)
      {
         // CODE starts here
      }

      cout << res << "\n";
   }
   return 0;
}
